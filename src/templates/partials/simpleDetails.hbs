{{#if (isArray details)}}
  <ul class="list-inside list-disc text-justify text-sm">
    {{#each details}}
      <li>
        {{this}}
      </li>
    {{/each}}
  </ul>
{{else}}
  <p class="text-justify text-sm">
    {{details}}
  </p>
{{/if}}